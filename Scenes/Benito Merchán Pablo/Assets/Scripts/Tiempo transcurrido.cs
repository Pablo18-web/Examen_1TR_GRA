using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class texto : MonoBehaviour
{
    [SerializeField] string Tiempotranscurrido;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
   // void Update() => Tiempotranscurrido.text = "TiempoTranscurrido"; //me da error con el . text y no entiendo muy bien el porque si es un serialize field de tipo string

    
    
}

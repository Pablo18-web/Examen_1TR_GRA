using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class crearcolumna : MonoBehaviour
{
    //te lo comento por el escrito porque me estoy desesperando y no lo veo y estoy muy agobiado a este codigo ni caso
    
    /*
    [SerializeField] GameObject Columna;
    [SerializeField] Transform RefPos;
    [SerializeField] float distobstacle;
    float posRamdom;
    float 
    // Start is called before the first frame update
    void Start()
    {
        StartCoroutine("ColumnCorrutine");
        InicioColumnas();
    }

    // Update is called once per frame
    void Update()
    {
        if (Input.GetButtonDown("Fire1"))
        {
            CrearColumna();
        }
    }
    void CrearColumna();
        float posRandom = Random.Range(0f, 30f);
        Vector3 DestPos = new Vector3(posRandom, 0, posRamdom);
        Vector3 NewPos = RefPos.position + DestPos;
         Instantiate(MyColumn, NewPos, Quaternion.identity);

    */






}
